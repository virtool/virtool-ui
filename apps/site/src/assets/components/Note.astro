---
const { color = "grey" } = Astro.props;

const colorClass = {
  blue: "bg-sky-100",
  grey: "bg-stone-100",
  red: "bg-red-100",
  yellow: "bg-yellow-100",
}[color];
---

<div class={`mt-2 mb-5 py-2 px-4 rounded-md font-medium ${colorClass}`}>
  <slot />
</div>
